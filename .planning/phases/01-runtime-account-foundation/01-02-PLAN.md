---
phase: 01-runtime-account-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - senso/package.json
  - senso/src/main.tsx
  - senso/src/App.tsx
  - senso/src/index.css
  - senso/src/lib/config.ts
  - senso/src/lib/api-client.ts
  - senso/src/features/auth/types.ts
  - senso/src/features/auth/storage.ts
  - senso/src/features/auth/session.ts
  - senso/src/features/auth/useAuth.ts
  - senso/src/features/auth/AuthScreen.tsx
  - senso/src/features/auth/AuthedHome.tsx
  - senso/src/features/auth/__tests__/auth-session.test.ts
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03]
must_haves:
  truths:
    - User can sign up and log in with email/password from the frontend.
    - User can trigger Google OAuth and is guided to email fallback when unavailable.
    - User remains signed in after refresh via localStorage token persistence and refresh flow.
  artifacts:
    - path: senso/src/features/auth/AuthScreen.tsx
      provides: Auth UI for email and Google sign-in actions
    - path: senso/src/features/auth/session.ts
      provides: Token storage, refresh orchestration, me check
    - path: senso/src/features/auth/__tests__/auth-session.test.ts
      provides: Automated session persistence and fallback behavior tests
  key_links:
    - from: senso/src/features/auth/session.ts
      to: api/app/api/auth.py
      via: calls to /auth/login, /auth/signup, /auth/refresh, /auth/me
      pattern: "\/auth\/(signup|login|refresh|me|google\/start)"
    - from: senso/src/features/auth/AuthScreen.tsx
      to: senso/src/features/auth/session.ts
      via: useAuth hook and action handlers
      pattern: "useAuth|login|signup|startGoogle"
---

<objective>
Implement frontend authentication experience and session persistence in Vite + React, wired to FastAPI auth endpoints.

Purpose: Deliver user-visible auth capabilities and persistent sessions required for Phase 1 demo readiness.
Output: Auth UI, token/session client logic, and automated frontend auth tests.
</objective>

<execution_context>
@/home/fulgidus/Documents/senso/.opencode/get-shit-done/workflows/execute-plan.md
@/home/fulgidus/Documents/senso/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-runtime-account-foundation/01-CONTEXT.md
@.planning/phases/01-runtime-account-foundation/01-UI-SPEC.md
@.planning/phases/01-runtime-account-foundation/01-01-PLAN.md

<interfaces>
From `senso/src/main.tsx`:
```tsx
createRoot(document.getElementById("root")!).render(
  <StrictMode>
    <ThemeProvider>
      <App />
    </ThemeProvider>
  </StrictMode>
)
```

From backend plan contract:
```text
POST /auth/signup
POST /auth/login
GET /auth/google/start
POST /auth/refresh
GET /auth/me
POST /auth/logout
```

From root config:
```json
"api": {
  "backendUrl": "http://localhost:8000"
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build auth client contracts and session persistence tests first</name>
  <files>senso/package.json, senso/src/lib/config.ts, senso/src/lib/api-client.ts, senso/src/features/auth/types.ts, senso/src/features/auth/storage.ts, senso/src/features/auth/session.ts, senso/src/features/auth/__tests__/auth-session.test.ts</files>
  <read_first>senso/package.json, config.json, .planning/phases/01-runtime-account-foundation/01-CONTEXT.md, .planning/phases/01-runtime-account-foundation/01-VALIDATION.md</read_first>
  <behavior>
    - Test 1: session bootstrap reads localStorage token and validates via `/auth/me`.
    - Test 2: expired access token triggers `/auth/refresh`, updates both tokens, retries `/auth/me`.
    - Test 3: Google start failure payload maps to fallback state consumed by UI.
  </behavior>
  <action>
    Add frontend auth interfaces and test infrastructure using vitest. Create typed auth models mirroring backend payloads. Implement config loader that reads backend base URL from root `config.json` via Vite env projection (do not hardcode URL). Write tests for localStorage persistence per D-06, short-lived + rotation flow per D-07, and fallback behavior per D-05. Keep token storage in localStorage by design (D-06) even though research suggests cookies.
  </action>
  <acceptance_criteria>
    - `senso/src/features/auth/storage.ts` contains `localStorage` read/write helpers for access and refresh tokens.
    - `senso/src/features/auth/__tests__/auth-session.test.ts` contains tests for bootstrap, refresh rotation, and google fallback mapping.
    - `pnpm --dir senso vitest run src/features/auth/__tests__/auth-session.test.ts` executes (RED first, then GREEN after implementation).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir senso vitest run src/features/auth/__tests__/auth-session.test.ts</automated>
  </verify>
  <done>Frontend auth contracts and persistence behavior are codified with executable tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement auth UI flow with Google fallback and persisted session gate</name>
  <files>senso/src/App.tsx, senso/src/main.tsx, senso/src/index.css, senso/src/features/auth/useAuth.ts, senso/src/features/auth/AuthScreen.tsx, senso/src/features/auth/AuthedHome.tsx, senso/src/features/auth/session.ts</files>
  <read_first>senso/src/App.tsx, senso/src/index.css, .planning/phases/01-runtime-account-foundation/01-UI-SPEC.md, senso/src/features/auth/session.ts</read_first>
  <action>
    Replace starter App with auth-aware shell: unauthenticated users see AuthScreen; authenticated users see AuthedHome + sign-out action. Apply UI-SPEC colors and copywriting contract exactly for empty/error states. Provide email signup/login form (D-03), Google sign-in trigger (D-05), and graceful fallback notice when backend returns `fallback: email_password`. On app startup, run session bootstrap that checks `/auth/me`, refreshes token when needed, and preserves login across refresh via localStorage (D-06, D-07, D-08).
  </action>
  <acceptance_criteria>
    - `senso/src/App.tsx` conditionally renders auth vs authenticated view based on hook state.
    - `senso/src/features/auth/AuthScreen.tsx` includes primary CTA copy `Create account` and fallback error copy from UI-SPEC.
    - `senso/src/features/auth/session.ts` includes calls to `/auth/me` and `/auth/refresh`.
    - `pnpm --dir senso typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir senso typecheck && pnpm --dir senso build</automated>
  </verify>
  <done>Frontend delivers complete Phase 1 auth user flow with resilient fallback and session persistence behavior.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validate full auth UX and refresh persistence behavior</name>
  <what-built>Vite React auth UI integrated with FastAPI for email/password, Google fallback, and persisted sessions.</what-built>
  <action>
    Execute full-flow manual verification after automated checks pass; capture any UX or fallback defects before phase execution continues.
  </action>
  <how-to-verify>
    1) Run frontend + backend locally.
    2) Create a new account via email/password and confirm authenticated view renders.
    3) Refresh browser; confirm user remains authenticated (no forced re-login).
    4) Trigger Google sign-in with provider intentionally unavailable and confirm fallback message appears while email/password remains usable.
    5) Sign out and confirm return to unauthenticated state.
  </how-to-verify>
  <acceptance_criteria>
    - Signup and login complete without console runtime errors.
    - Refresh keeps authenticated session active.
    - Google-unavailable path shows fallback copy and does not block email login.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir senso typecheck && pnpm --dir senso build</automated>
  </verify>
  <done>Human confirms the integrated auth flow behavior is acceptable for Phase 1 execution.</done>
  <resume-signal>Type "approved" to proceed, or describe defects to fix.</resume-signal>
</task>

</tasks>

<verification>
Run `pnpm --dir senso vitest run src/features/auth/__tests__/auth-session.test.ts && pnpm --dir senso typecheck && pnpm --dir senso build`.
</verification>

<success_criteria>
- `AUTH-01` frontend path (signup/login) is complete and operational.
- `AUTH-02` Google flow includes required fallback behavior.
- `AUTH-03` session remains active after page refresh through token persistence + refresh orchestration.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-account-foundation/01-runtime-account-foundation-02-SUMMARY.md`
</output>
