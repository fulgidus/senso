---
phase: 01-runtime-account-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - docker-compose.yml
  - api/Dockerfile
  - senso/Dockerfile
  - scripts/dev-up.sh
  - scripts/smoke-auth.sh
  - .env.example
  - README.md
autonomous: true
requirements: [DEMO-03]
must_haves:
  truths:
    - A fresh machine can start required Phase 1 services with one command.
    - Frontend and API are reachable on documented local URLs after startup.
    - Reproducible setup documentation is sufficient for judges to run without tribal knowledge.
  artifacts:
    - path: docker-compose.yml
      provides: one-command orchestration for frontend, api, postgres
    - path: README.md
      provides: exact local run and verification commands
    - path: scripts/smoke-auth.sh
      provides: executable auth runtime smoke checks
  key_links:
    - from: docker-compose.yml
      to: api/Dockerfile
      via: api service build config
      pattern: "api:|build:.*api"
    - from: docker-compose.yml
      to: senso/Dockerfile
      via: frontend service build config
      pattern: "frontend:|build:.*senso"
---

<objective>
Deliver reproducible local runtime and startup docs that satisfy judging-ready one-command setup.

Purpose: Fulfill DEMO-03 with deterministic local execution independent of developer machine drift.
Output: Docker Compose stack, env template, smoke script, and README runbook.
</objective>

<execution_context>
@/home/fulgidus/Documents/senso/.opencode/get-shit-done/workflows/execute-plan.md
@/home/fulgidus/Documents/senso/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-runtime-account-foundation/01-CONTEXT.md
@config.json
@senso/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Dockerfiles and compose topology for one-command startup</name>
  <files>docker-compose.yml, api/Dockerfile, senso/Dockerfile, .env.example</files>
  <read_first>config.json, senso/package.json, .planning/phases/01-runtime-account-foundation/01-CONTEXT.md</read_first>
  <action>
    Implement Docker Compose runtime per D-09 with services `frontend`, `api`, and `postgres`. Wire frontend to backend URL from compose network, and backend to Postgres DSN using env vars. Add `.env.example` containing required variables for auth (JWT secret, token TTLs, Google OAuth keys, database URL). Do not add VPS/deployment automation artifacts (D-10 deferred). Ensure service commands are non-interactive and ready for fresh-machine startup.
  </action>
  <acceptance_criteria>
    - `docker-compose.yml` contains `frontend`, `api`, and `postgres` service definitions.
    - `.env.example` contains `JWT_SECRET=`, `ACCESS_TOKEN_TTL_MINUTES=`, `REFRESH_TOKEN_TTL_DAYS=`, `GOOGLE_CLIENT_ID=`, `GOOGLE_CLIENT_SECRET=`.
    - `docker compose config` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>docker compose config</automated>
  </verify>
  <done>Containerized runtime exists and can be resolved by Docker Compose without config errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add startup/smoke scripts and judge-focused runtime documentation</name>
  <files>scripts/dev-up.sh, scripts/smoke-auth.sh, README.md</files>
  <read_first>README.md, docker-compose.yml, .planning/ROADMAP.md</read_first>
  <action>
    Add `scripts/dev-up.sh` wrapping one-command startup (`docker compose up --build`) and `scripts/smoke-auth.sh` verifying API health plus auth route reachability. Update root README with exact bootstrap flow for fresh machine: clone, copy `.env.example` to `.env`, run one command, open frontend URL, execute smoke command. Include explicit expected URLs and outputs for verification.
  </action>
  <acceptance_criteria>
    - `README.md` contains a section heading `## Phase 1 Local Runtime`.
    - `README.md` includes exact command `docker compose up --build`.
    - `scripts/smoke-auth.sh` checks at least one auth endpoint and exits non-zero on failure.
    - `bash scripts/dev-up.sh --help` or script invocation usage exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/smoke-auth.sh</automated>
  </verify>
  <done>Fresh-machine runbook and smoke checks confirm reproducible setup suitable for judging.</done>
</task>

</tasks>

<verification>
Run `docker compose config` and `bash scripts/smoke-auth.sh`; both must succeed from clean checkout after env setup.
</verification>

<success_criteria>
- `DEMO-03` satisfied by documented one-command local startup and runnable smoke checks.
- No out-of-scope deployment automation introduced (D-10 respected).
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-account-foundation/01-runtime-account-foundation-03-SUMMARY.md`
</output>
