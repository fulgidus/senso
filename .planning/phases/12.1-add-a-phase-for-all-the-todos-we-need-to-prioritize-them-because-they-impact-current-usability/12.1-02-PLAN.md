---
phase: 12.1-add-a-phase-for-all-the-todos-we-need-to-prioritize-them-because-they-impact-current-usability
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - senso/src/features/coaching/ChatScreen.tsx
autonomous: true
requirements:
  - TODO-24
  - TODO-25
must_haves:
  truths:
    - "In dark mode, the coach picker shows persona cards with the persona's dark theme colors, not light theme colors"
    - "Switching to a different coach persona takes effect for the current conversation and stays selected when typing and sending messages"
    - "Starting a new conversation (new chat button) does NOT reset the currently selected persona — the user's in-session choice is preserved"
  artifacts:
    - path: "senso/src/features/coaching/ChatScreen.tsx"
      provides: "Fixed PersonaSwitcher theme usage + persona persistence across new conversations"
      contains: "resolvedTheme"
  key_links:
    - from: "PersonaSwitcher render"
      to: "getResolvedTheme / getPersonaTheme"
      via: "resolvedTheme prop passed to PersonaSwitcher"
      pattern: "resolvedTheme"
    - from: "handleNewConversation"
      to: "activePersonaId"
      via: "setActivePersonaId NOT called in handleNewConversation"
      pattern: "handleNewConversation"
---

<objective>
Fix two P1 hackathon-critical bugs in ChatScreen: the coach picker rendering with wrong (light) theme colors in dark mode (#24), and the coach picker resetting to the default persona every time a new conversation starts (#25).

Purpose: Both bugs are highly visible during a live demo. A judge switching to a dark theme will immediately see the broken coach picker. And if they switch coach mid-demo then click "New Chat", the persona silently resets — the demo breaks.

Output: Two targeted fixes to `ChatScreen.tsx` — no new files, no new dependencies.
</objective>

<execution_context>
@/home/fulgidus/Documents/senso/.opencode/get-shit-done/workflows/execute-plan.md
@/home/fulgidus/Documents/senso/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<interfaces>
<!-- Key code facts extracted from ChatScreen.tsx -->

Line 9: `import { useTheme } from "@/components/theme-provider"`
Line 892: `function getResolvedTheme(theme: "light" | "dark" | "system"): "light" | "dark"` — uses `document.documentElement.classList.contains("dark")` to resolve "system"
Line 900: `function getPersonaTheme(persona, theme)` — returns `theme === "dark" ? persona.theme.dark : persona.theme.light`
Line 955: `const { theme } = useTheme()`
Line 981: `const resolvedTheme = useMemo(() => getResolvedTheme(theme), [theme])`
Line 1731: `resolvedTheme={resolvedTheme}` — already passed to the MessageBubble sub-component

Line 522 (BUG #24): `const theme = persona.theme?.light` — always uses light theme regardless of `resolvedTheme`

Line 1191: `const handleNewConversation = useCallback(async () => {`
Line 1196 (BUG #25): `setActivePersonaId(user.defaultPersonaId ?? "mentore-saggio")` — resets to default on every new chat

Line 1170–1174: The `useEffect` that syncs `sessionId` changes already sets `activePersonaId` to `user.defaultPersonaId` — this is the correct behavior for loading a saved session from history, not for creating a new one.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix PersonaSwitcher dark mode theme (#24)</name>
  <files>senso/src/features/coaching/ChatScreen.tsx</files>
  <action>
The `PersonaSwitcher` inner render function (around line 520) has:
```typescript
const theme = persona.theme?.light
```

This hard-codes the light theme colors for the persona button styles regardless of the current color scheme.

**Fix:** The `PersonaSwitcher` component already receives `resolvedTheme` as a prop (it's typed in the props interface at line 614). Use `getPersonaTheme(persona, resolvedTheme)` instead of `persona.theme?.light`.

Change:
```typescript
const theme = persona.theme?.light
```

To:
```typescript
const theme = getPersonaTheme(persona, resolvedTheme)
```

`getPersonaTheme` is already defined at line 900 in the same file — no imports needed. `resolvedTheme` is already in the component's props scope.

Verify the fix covers all three style usages of `theme` immediately below this line:
- `borderColor: selected ? theme?.bubble_border ?? "var(--primary)" : undefined`
- `backgroundColor: selected ? theme?.bubble_bg ?? undefined : undefined`
- `style={{ backgroundColor: theme?.avatar_bg }}`
  </action>
  <verify>
    <automated>docker compose run --rm frontend pnpm tsc --noEmit 2>&1 | grep -E "ChatScreen|error TS" || echo "TYPE CHECK PASSED"</automated>
  </verify>
  <done>
    - PersonaSwitcher uses `getPersonaTheme(persona, resolvedTheme)` instead of `persona.theme?.light`
    - In dark mode, persona cards show dark theme colors (darker backgrounds, appropriate borders)
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Preserve selected persona across new conversations (#25)</name>
  <files>senso/src/features/coaching/ChatScreen.tsx</files>
  <action>
The `handleNewConversation` callback (around line 1191–1207) calls:
```typescript
setActivePersonaId(user.defaultPersonaId ?? "mentore-saggio")
```

This resets the persona to the user's default every time the user starts a new conversation, silently overriding any in-session persona selection they made.

**Fix:** Remove the `setActivePersonaId(...)` call from `handleNewConversation`. The active persona should NOT change when starting a new conversation — it should carry over the current selection.

Also update the `useCallback` dependency array for `handleNewConversation` to remove `user.defaultPersonaId` if it was only referenced by the removed line. Check the full callback body to see if `user.defaultPersonaId` is still used elsewhere in that callback. If not, remove it from the `[...]` deps array.

**Note:** The `useEffect` at lines 1170–1174 that syncs persona when `sessionId` changes is a separate concern (loading saved sessions from history — correct behavior). Do NOT change that effect.
  </action>
  <verify>
    <automated>docker compose run --rm frontend pnpm tsc --noEmit 2>&1 | grep -E "ChatScreen|error TS" || echo "TYPE CHECK PASSED"</automated>
  </verify>
  <done>
    - `handleNewConversation` no longer calls `setActivePersonaId`
    - After switching to a non-default coach and clicking "New Chat", the selected coach stays selected
    - TypeScript compiles cleanly
    - The session-load useEffect is unchanged
  </done>
</task>

</tasks>

<verification>
- TypeScript: `pnpm tsc --noEmit` in frontend container — zero errors
- Visual (dark mode): Open coach picker in dark theme — cards should show dark persona colors (darker bg)
- Visual (persona persistence): Switch to "Amico Diretto" persona → send a message → click New Chat → persona should still be "Amico Diretto" not reset to default
</verification>

<success_criteria>
- Dark mode coach picker is readable and uses correct dark theme colors
- Coach selection persists through new conversation creation
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-add-a-phase-for-all-the-todos-we-need-to-prioritize-them-because-they-impact-current-usability/12.1-02-SUMMARY.md`
</output>
