---
phase: "12.1"
plan: "08"
subsystem: "frontend-ui"
tags: ["ui-polish", "charts", "dark-mode", "responsive", "tts", "a11y", "mobile"]
dependency_graph:
  requires: []
  provides: ["pie-chart-spending", "dark-mode-learn-cards", "tts-fallback-badge", "responsive-admin-tables"]
  affects: ["ProfileScreen", "ChatScreen", "ContentAdminPage", "MerchantMapAdminPage", "ModerationQueuePage", "FileList"]
tech_stack:
  added: []
  patterns: ["recharts PieChart donut", "Tailwind hidden sm:block / sm:hidden pattern", "relative inline-flex amber badge"]
key_files:
  created: []
  modified:
    - "senso/src/features/profile/ProfileScreen.tsx"
    - "senso/src/features/coaching/ChatScreen.tsx"
    - "senso/src/features/admin/ContentAdminPage.tsx"
    - "senso/src/features/admin/MerchantMapAdminPage.tsx"
    - "senso/src/features/admin/ModerationQueuePage.tsx"
    - "senso/src/features/ingestion/FileList.tsx"
    - "senso/src/i18n/locales/it.json"
    - "senso/src/i18n/locales/en.json"
decisions:
  - "Merged hidden sm:block and overflow-x-auto into one div per table to keep nesting minimal"
  - "Used ttsFallbackHint (new key) for VoicePlayButton title, keeping ttsFallbackActive for other uses"
  - "Pre-existing div imbalance in ContentAdminPage.tsx (38/37 before changes) left as-is per scope rules"
metrics:
  duration: "~16 minutes"
  completed: "2026-04-02T10:36:00Z"
  tasks_completed: 2
  tasks_total: 2
  files_modified: 8
---

# Phase 12.1 Plan 08: UI Polish - Pie Chart, Dark-Mode Cards, TTS Badge, Responsive Tables Summary

**One-liner:** Replaced spending BarChart with recharts PieChart donut, fixed LearnCardStub dark-mode blue hardcodes with semantic tokens, added amber TTS fallback dot badge, and added `sm:hidden` mobile card layouts to four admin/ingestion tables.

## Tasks Completed

| #   | Task                                                   | Commit    | Key Files                                                                                             |
| --- | ------------------------------------------------------ | --------- | ----------------------------------------------------------------------------------------------------- |
| 1   | Pie chart + dark-mode coaching cards + TTS i18n keys   | `95d29d0` | ProfileScreen.tsx, ChatScreen.tsx, it.json, en.json                                                   |
| 2   | TTS fallback badge + responsive admin/ingestion tables | `37e3249` | ChatScreen.tsx, ContentAdminPage.tsx, MerchantMapAdminPage.tsx, ModerationQueuePage.tsx, FileList.tsx |

## What Was Built

### Fix #12 - PieChart Donut (ProfileScreen)
- Replaced vertical `BarChart` with `PieChart` donut (`innerRadius="55%" outerRadius="75%"`) in the spending breakdown section
- Removed the category pills section (redundant with `<Legend>`)
- Updated recharts import to include `Pie`, `PieChart`, `Legend` while retaining `Bar`, `BarChart`, etc. for the income vs expenses chart

### Fix #23 - Dark-mode LearnCardStub (ChatScreen)
- Replaced hardcoded `border-blue-200 bg-blue-50 text-blue-800 text-blue-700 text-blue-600` with semantic Tailwind tokens: `border-primary/30 bg-primary/5 text-primary text-foreground text-muted-foreground`
- Cards now respect light/dark mode automatically

### TTS i18n keys
- Added `ttsFallbackBadge` and `ttsFallbackHint` to both `it.json` and `en.json` under the `coaching` section

### Fix #22 - TTS Fallback Amber Dot Badge (ChatScreen)
- Wrapped `VoicePlayButton`'s `<Button>` in `<div className="relative inline-flex items-center">`
- Added `<span className="absolute -top-0.5 -right-0.5 h-2 w-2 rounded-full bg-amber-400">` conditionally when `usingFallback=true`
- Updated `title` to use new `ttsFallbackHint` key (more descriptive than old `ttsFallbackActive`)

### Fix #16 - Responsive Tables (4 files)
All four tables now have:
- `sm:hidden` mobile card list above the table (stacked card per row)
- `hidden sm:block overflow-x-auto` on the original table wrapper

| File                       | Mobile card shows                                                                                                                       |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| `ContentAdminPage.tsx`     | title, slug, type badge, locale, publish toggle, date, edit/delete buttons                                                              |
| `MerchantMapAdminPage.tsx` | description_raw, canonical merchant, method badge, confidence badge, category, blacklist/unblacklist action + inline textarea expansion |
| `ModerationQueuePage.tsx`  | user id + date, content type badge, violation tags, severity badge, action taken, confirm/revert buttons                                |
| `FileList.tsx`             | filename + confirmed checkmark, content type, extraction method cell, inspect/retry/confirm/report/remove buttons                       |

## Deviations from Plan

### Auto-fixed Issues

None - plan executed exactly as written with the following minor adaptations:

1. **[Discovery] Merged `hidden sm:block` + `overflow-x-auto` into one div** - Rather than two nested divs, classes were combined on the existing table wrapper div. This keeps the DOM flatter.

2. **[Discovery] Used `ttsFallbackHint` instead of `ttsFallbackActive` for VoicePlayButton title** - The plan specified using the new `ttsFallbackHint` key (added in Task 1) for the button title, replacing the existing `ttsFallbackActive` reference. Done as specified.

### Out-of-scope Discovery

- **Pre-existing div imbalance in `ContentAdminPage.tsx`**: The original file had 38 `<div` opens vs 37 `</div>` closes - 1 unclosed div. This pre-dates these changes (verified via `git show HEAD`). Not fixed per scope boundary rules. Logged here for awareness.

## Known Stubs

None - all changes are UI display wiring with real data sources.

## Verification Notes

- TypeScript verification via `docker compose run --rm frontend pnpm tsc --noEmit` was not run - Docker CLI unavailable in executor environment. This is a known constraint documented in STATE.md.
- Div balance verified manually for all 4 files via `node -e` script; FileList, MerchantMapAdminPage, ModerationQueuePage all balanced (OK). ContentAdminPage imbalance confirmed as pre-existing.
- All recharts imports verified as used via grep.

## Self-Check: PASSED

- [x] `senso/src/features/profile/ProfileScreen.tsx` - modified, PieChart in place
- [x] `senso/src/features/coaching/ChatScreen.tsx` - modified, LearnCardStub + VoicePlayButton updated
- [x] `senso/src/i18n/locales/it.json` - ttsFallbackBadge + ttsFallbackHint added
- [x] `senso/src/i18n/locales/en.json` - ttsFallbackBadge + ttsFallbackHint added
- [x] `senso/src/features/admin/ContentAdminPage.tsx` - mobile cards + hidden sm:block table
- [x] `senso/src/features/admin/MerchantMapAdminPage.tsx` - mobile cards + hidden sm:block table
- [x] `senso/src/features/admin/ModerationQueuePage.tsx` - mobile cards + hidden sm:block table
- [x] `senso/src/features/ingestion/FileList.tsx` - mobile cards + hidden sm:block table
- [x] Commit `95d29d0` exists (Task 1)
- [x] Commit `37e3249` exists (Task 2)
