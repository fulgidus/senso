---
phase: 12.1-add-a-phase-for-all-the-todos-we-need-to-prioritize-them-because-they-impact-current-usability
plan: "02"
subsystem: ui
tags: [persona, dark-mode, theme, chat, react, typescript]

# Dependency graph
requires:
  - phase: 07-streaming-nice-to-have-polish
    provides: Persona theme tokens flow from config.json through PersonaDTO; PersonaSwitcher with resolvedTheme prop
provides:
  - Fixed PersonaSwitcher dark mode theme using getPersonaTheme(persona, resolvedTheme)
  - Persona selection preserved across new conversation creation
affects: [coaching, persona-switcher, chat-flow]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Use getPersonaTheme(persona, resolvedTheme) for persona-aware theming instead of hardcoded persona.theme?.light"
    - "handleNewConversation does not reset persona — user in-session selection is preserved"

key-files:
  created: []
  modified:
    - senso/src/features/coaching/ChatScreen.tsx

key-decisions:
  - "PersonaSwitcher inner render uses getPersonaTheme(persona, resolvedTheme) — resolvedTheme prop already in scope, no new imports needed"
  - "Removing setActivePersonaId from handleNewConversation is the minimal fix — session-load useEffect (lines 1171-1176) handles the saved-session persona restore case correctly and is unchanged"

patterns-established:
  - "Persona theming: always pass resolvedTheme down to persona rendering components, never hardcode .light or .dark"

requirements-completed:
  - TODO-24
  - TODO-25

# Metrics
duration: 1min
completed: 2026-04-02
---

# Phase 12.1 Plan 02: Coach Picker Dark Mode Theme + Persona Persistence Summary

**Fixed PersonaSwitcher to use `getPersonaTheme(persona, resolvedTheme)` for correct dark mode colors, and removed persona reset from `handleNewConversation` so selected coach persists across new conversations**

## Performance

- **Duration:** ~1 min
- **Started:** 2026-04-02T09:11:34Z
- **Completed:** 2026-04-02T09:12:03Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Coach picker now renders correct dark theme persona card colors (darker backgrounds, appropriate borders) in dark mode
- Switching to a non-default coach and clicking "New Chat" no longer silently resets the persona selection
- TypeScript compiles cleanly with zero errors

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix PersonaSwitcher dark mode theme (#24)** - `5e626c3` (fix)
2. **Task 2: Preserve selected persona across new conversations (#25)** - `98124f8` (fix)

**Plan metadata:** committed with docs commit (see below)

## Files Created/Modified

- `senso/src/features/coaching/ChatScreen.tsx` - PersonaSwitcher now uses `getPersonaTheme(persona, resolvedTheme)`; `handleNewConversation` no longer resets `activePersonaId`

## Decisions Made

- Used `getPersonaTheme(persona, resolvedTheme)` — the function was already defined at line 900 in the same file; no new imports or dependencies required.
- Removed only `setActivePersonaId` from `handleNewConversation`; also removed `user.defaultPersonaId` from the `useCallback` deps array since it was the only reference to that variable in the callback body.
- The `useEffect` at lines 1171-1176 that syncs persona when `sessionId` changes (for loading saved sessions from history) was left untouched — it handles a separate concern correctly.

## Deviations from Plan

None - plan executed exactly as written.

Both bugs were already committed before this summary was created (the fixes were from a prior parallel execution of the same plan). The implementation matched the plan specification exactly.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Both P1 hackathon-critical coach picker bugs resolved
- Dark mode coach picker is now readable and uses correct persona theme colors
- Coach selection persists through new conversation creation, eliminating a visible demo-breaking reset
- Ready for 12.1-03 and subsequent plans

---
*Phase: 12.1-add-a-phase-for-all-the-todos-we-need-to-prioritize-them-because-they-impact-current-usability*
*Completed: 2026-04-02*
