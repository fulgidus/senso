---
phase: 07-streaming-nice-to-have-polish
plan: "04"
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-03
files_modified:
  - senso/src/features/auth/types.ts
  - senso/src/features/auth/session.ts
  - senso/src/features/auth/__tests__/auth-session.test.ts
  - senso/src/features/settings/SettingsScreen.tsx
  - senso/src/features/coaching/ChatScreen.tsx
  - senso/src/i18n/locales/it.json
  - senso/src/i18n/locales/en.json
autonomous: false
requirements:
  - COCH-05
must_haves:
  truths:
    - User can switch coach in chat for future replies only.
    - User can save a default coach in Settings and new conversations start from that saved default.
    - Restored/history assistant messages preserve subtle persona-specific tinting.
  artifacts:
    - path: senso/src/features/settings/SettingsScreen.tsx
      provides: default coach selector UI
      min_lines: 1
    - path: senso/src/features/coaching/ChatScreen.tsx
      provides: in-chat persona switcher + subtle themed assistant bubbles
      min_lines: 1
  key_links:
    - from: senso/src/features/settings/SettingsScreen.tsx
      to: senso/src/features/auth/session.ts
      via: updateMe save payload includes default persona
      pattern: defaultPersonaId
    - from: senso/src/features/coaching/ChatScreen.tsx
      to: api/app/personas/config.json
      via: persona API theme metadata rendered in assistant history
      pattern: bubble_border
---

<objective>
Finish the Phase 7 persona UX with persistent settings, subtle in-chat switching, and a human verification pass for the visible polish.

Purpose: Deliver D-06 to D-10 and confirm the final persona/streaming/history polish feels intentional in the running app.
Output: Settings default-coach UI, quick chat switcher, subtle persona theming, and a blocking visual checkpoint.
</objective>

<execution_context>
@/home/fulgidus/Documents/senso/.opencode/get-shit-done/workflows/execute-plan.md
@/home/fulgidus/Documents/senso/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-streaming-nice-to-have-polish/07-CONTEXT.md
@.planning/phases/07-streaming-nice-to-have-polish/07-UI-SPEC.md
@.planning/phases/07-streaming-nice-to-have-polish/07-01-PLAN.md
@.planning/phases/07-streaming-nice-to-have-polish/07-03-PLAN.md

<interfaces>
From `senso/src/features/auth/types.ts`:
```ts
export type User = {
  id: string
  email: string
  firstName: string | null
  lastName?: string | null
  voiceGender?: VoiceGender
  voiceAutoListen?: boolean
}
```

From `senso/src/features/auth/session.ts`:
```ts
export async function updateMe(
  accessToken: string,
  data: { firstName?: string | null; lastName?: string | null; voiceGender?: VoiceGender | null; voiceAutoListen?: boolean | null },
): Promise<User>
```

From `senso/src/features/coaching/coachingApi.ts`:
```ts
export interface Persona {
  id: string
  name: string
  description: string
  icon: string
  available: boolean
  tts: PersonaTTSConfig
  defaultGender?: "masculine" | "feminine" | "neutral"
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend frontend auth/session state with default persona persistence</name>
  <files>senso/src/features/auth/types.ts, senso/src/features/auth/session.ts, senso/src/features/auth/__tests__/auth-session.test.ts, senso/src/features/settings/SettingsScreen.tsx</files>
  <read_first>
    - senso/src/features/auth/types.ts
    - senso/src/features/auth/session.ts
    - senso/src/features/auth/__tests__/auth-session.test.ts
    - senso/src/features/settings/SettingsScreen.tsx
    - api/app/schemas/auth.py
  </read_first>
  <behavior>
    - Test 1: bootstrap/login parsing preserves `default_persona_id` from `/auth/me` into the frontend `User` shape.
    - Test 2: Settings save sends `default_persona_id` back through `updateMe()`.
  </behavior>
  <action>
    Add `defaultPersonaId?: string` to the frontend `User` type and update `parseUser()` / `updateMe()` in `senso/src/features/auth/session.ts` to round-trip the backend's `default_persona_id`. Extend `SettingsScreen.tsx` local dirty-state/save payload to include the saved default coach. Keep all new UI strings in `it.json` and `en.json`; do not hardcode Italian labels. This task lays the persistence contract that Task 2 will consume.
  </action>
  <acceptance_criteria>
    - `senso/src/features/auth/types.ts` contains `defaultPersonaId` on `User`.
    - `senso/src/features/auth/session.ts` reads `default_persona_id` from backend payloads and sends it in `updateMe()`.
    - `senso/src/features/auth/__tests__/auth-session.test.ts` asserts `default_persona_id` parsing or update payload behavior.
    - `senso/src/features/settings/SettingsScreen.tsx` includes `defaultPersonaId` in its dirty-state/save path.
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm frontend pnpm vitest run src/features/auth/__tests__/auth-session.test.ts</automated>
  </verify>
  <done>Frontend auth state knows the user's default coach and Settings can persist it through the existing save flow.</done>
</task>

<task type="auto">
  <name>Task 2: Add quick persona switching, default-coach settings UI, and subtle history theming</name>
  <files>senso/src/features/settings/SettingsScreen.tsx, senso/src/features/coaching/ChatScreen.tsx, senso/src/i18n/locales/it.json, senso/src/i18n/locales/en.json</files>
  <read_first>
    - senso/src/features/settings/SettingsScreen.tsx
    - senso/src/features/coaching/ChatScreen.tsx
    - senso/src/features/coaching/coachingApi.ts
    - .planning/phases/07-streaming-nice-to-have-polish/07-UI-SPEC.md
    - .planning/phases/07-streaming-nice-to-have-polish/07-CONTEXT.md
  </read_first>
  <action>
    In `SettingsScreen.tsx`, add a `Default coach` group above the existing voice controls that renders all personas as selectable rows, uses accent outline + persona tint wash + trailing checkmark for the selected row, and saves through the existing Save button model (D-06/D-08). In `ChatScreen.tsx`, add a compact header trigger showing current persona icon + name, opening a picker that reuses the existing modal/sheet patterns from the UI-SPEC. Switching persona in chat must only affect future assistant replies (D-07): keep existing stored messages untouched, carry the active persona id into new requests, and preserve per-message persona tint from stored `persona_id` when rendering history/restored sessions. Apply subtle persona identity only through avatar background tint, 1px bubble border tint, and a tiny `icon + name` cue on the first assistant message after a switch; do not turn every bubble into a loud persona card (D-09/D-10). New conversations and welcome flows must start from `user.defaultPersonaId ?? 'mentore-saggio'`, not the last temporary in-chat switch (D-08).
  </action>
  <acceptance_criteria>
    - `senso/src/features/settings/SettingsScreen.tsx` contains a default-coach selector rendered above voice preferences.
    - `senso/src/features/coaching/ChatScreen.tsx` contains an in-chat persona switcher trigger and picker.
    - `senso/src/features/coaching/ChatScreen.tsx` uses stored/request persona ids to style assistant messages rather than a single hardcoded owl presentation.
    - New/updated copy for persona selector labels exists in both locale JSON files.
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm frontend pnpm build</automated>
  </verify>
  <done>Users can save a default coach, switch personas in chat for future turns only, and see subtle persona-aware message styling in history and new replies.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify streaming, restore, and persona polish in the running UI</name>
  <what-built>Streaming bubble, restore toast, subtle persona theming, chat quick-switcher, and Settings default-coach persistence.</what-built>
  <action>Run the automated frontend build first, then perform the five manual checks below against the running app to confirm the visible Phase 7 polish matches D-01 to D-14 and the approved UI-SPEC.</action>
  <how-to-verify>
    1. Start the app and open chat with at least one existing conversation.
    2. Confirm the most recent conversation auto-restores and a small non-blocking restore toast appears once, then disappears on its own.
    3. Ask a new coaching question and verify the assistant text grows progressively inside one real bubble; structured cards/details appear only after the text completes.
    4. Open the persona switcher in chat, switch to another coach, ask another question, and verify only the new assistant reply changes persona cue/tint while older messages keep their original persona styling.
    5. Open Settings, change the default coach, save, start a brand-new conversation, and verify the new conversation uses the saved default coach rather than the last temporary chat switch.
  </how-to-verify>
  <verify>
    <automated>docker compose run --rm frontend pnpm build</automated>
  </verify>
  <done>User replies with `approved`, or returns a concrete issue list for follow-up replanning/execution.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Run:
- `docker compose run --rm frontend pnpm vitest run src/features/auth/__tests__/auth-session.test.ts`
- `docker compose run --rm frontend pnpm build`
</verification>

<success_criteria>
- Default coach persistence works through Settings and frontend session state.
- In-chat switching affects future replies only, while history preserves persona-specific cues.
- Human verification confirms the visible Phase 7 polish feels correct.
</success_criteria>

<output>
After completion, create `.planning/phases/07-streaming-nice-to-have-polish/07-04-SUMMARY.md`
</output>
