---
phase: 07-streaming-nice-to-have-polish
plan: "03"
type: execute
wave: 2
depends_on:
  - 07-02
files_modified:
  - senso/src/features/coaching/coachingApi.ts
  - senso/src/features/coaching/ChatScreen.tsx
  - senso/src/features/coaching/useVoiceMode.ts
  - senso/src/i18n/locales/it.json
  - senso/src/i18n/locales/en.json
autonomous: true
requirements:
  - COCH-05
must_haves:
  truths:
    - The assistant reply appears progressively inside a real chat bubble and falls back silently to the full-response path if streaming fails.
    - Returning to chat restores the latest conversation, shows the full history, and confirms continuity with a subtle toast.
  artifacts:
    - path: senso/src/features/coaching/coachingApi.ts
      provides: frontend SSE client contract + fallback-safe chat helpers
      min_lines: 1
    - path: senso/src/features/coaching/ChatScreen.tsx
      provides: streaming bubble, restore toast, scroll behavior
      min_lines: 1
  key_links:
    - from: senso/src/features/coaching/coachingApi.ts
      to: /coaching/chat/stream
      via: fetch-based SSE reader
      pattern: chat/stream
    - from: senso/src/features/coaching/ChatScreen.tsx
      to: senso/src/features/coaching/useVoiceMode.ts
      via: TTS only after final streamed reply
      pattern: onAssistantMessage
---

<objective>
Wire the frontend streaming experience and restoration polish on top of the new SSE backend contract.

Purpose: Deliver the visible Phase 7 UX changes for D-01 to D-05 and D-11 to D-14 without breaking the existing chat, history, or voice loop.
Output: Streaming client path, polished in-bubble progressive text, restore toast, and updated scroll/fallback behavior.
</objective>

<execution_context>
@/home/fulgidus/Documents/senso/.opencode/get-shit-done/workflows/execute-plan.md
@/home/fulgidus/Documents/senso/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-streaming-nice-to-have-polish/07-CONTEXT.md
@.planning/phases/07-streaming-nice-to-have-polish/07-RESEARCH.md
@.planning/phases/07-streaming-nice-to-have-polish/07-UI-SPEC.md
@.planning/phases/06-learn-act-cards-demo-hardening/06-04-SUMMARY.md

<interfaces>
From `senso/src/features/coaching/coachingApi.ts`:
```ts
export async function sendMessage(
  message: string,
  locale: "it" | "en" = "it",
  personaId: string = "mentore-saggio",
  sessionId?: string,
): Promise<CoachingResponse>
```

From `senso/src/features/coaching/ChatScreen.tsx`:
```ts
interface DisplayMessage {
  role: "user" | "assistant"
  content: string
  response?: CoachingResponse
  isWelcome?: boolean
}
```

From `senso/src/features/coaching/useVoiceMode.ts`:
```ts
onAssistantMessage: (text: string) => void
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a frontend SSE client with silent full-response fallback</name>
  <files>senso/src/features/coaching/coachingApi.ts, senso/src/i18n/locales/it.json, senso/src/i18n/locales/en.json</files>
  <read_first>
    - senso/src/features/coaching/coachingApi.ts
    - senso/src/features/auth/storage.ts
    - senso/src/lib/api-client.ts
    - .planning/phases/07-streaming-nice-to-have-polish/07-02-PLAN.md
  </read_first>
  <behavior>
    - Test 1: the streaming client can parse `meta`, repeated `delta`, `final`, and `done` events from an SSE response body.
    - Test 2: when the stream fails before `final`, the caller can fall back to `sendMessage()` without changing the response contract.
  </behavior>
  <action>
    Extend `senso/src/features/coaching/coachingApi.ts` with a fetch-based SSE helper for `POST /coaching/chat/stream` that uses the existing Bearer token, request body `{ message, session_id, locale, persona_id }`, and `text/event-stream` parsing. Do not use native `EventSource`; it cannot send the current auth header and POST body. Export a concrete helper that surfaces streaming callbacks such as `onMeta`, `onDelta`, and `onFinal`, plus a promise rejection path for `ChatScreen` fallback. Keep the existing `sendMessage()` API unchanged as the D-03 silent fallback path. Add any new i18n keys needed for a restore toast / interrupted-stream copy in both locale JSON files; do not hardcode user-facing text.
  </action>
  <acceptance_criteria>
    - `senso/src/features/coaching/coachingApi.ts` contains a dedicated `/coaching/chat/stream` client path.
    - The parser handles literal event names `meta`, `delta`, `final`, and `done`.
    - Existing `sendMessage()` remains exported and unchanged as a full-response fallback.
    - Any new user-facing copy exists in both `senso/src/i18n/locales/it.json` and `senso/src/i18n/locales/en.json`.
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm frontend pnpm build</automated>
  </verify>
  <done>The frontend has a typed SSE client contract ready for ChatScreen, and the legacy full-response client still works as the silent fallback path.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the loading skeleton with a real streaming bubble and restore toast UX</name>
  <files>senso/src/features/coaching/ChatScreen.tsx, senso/src/features/coaching/useVoiceMode.ts, senso/src/i18n/locales/it.json, senso/src/i18n/locales/en.json</files>
  <read_first>
    - senso/src/features/coaching/ChatScreen.tsx
    - senso/src/features/coaching/useVoiceMode.ts
    - senso/src/features/coaching/VoiceModeBar.tsx
    - .planning/phases/07-streaming-nice-to-have-polish/07-UI-SPEC.md
    - .planning/phases/06-learn-act-cards-demo-hardening/06-04-SUMMARY.md
  </read_first>
  <action>
    Update `ChatScreen.tsx` so submit flow appends the user bubble immediately, mounts a real assistant bubble shell within the existing chat list, and fills `msg.content` progressively from SSE `delta` events instead of using the current skeleton-only wait state. Preserve D-02 by withholding `action_cards`, `resource_cards`, `learn_cards`, `affordability_verdict`, and `details_a2ui` until the `final` event arrives, then insert them below the completed text with a restrained fade/slide-up treatment per the UI-SPEC. If the stream handshake fails or dies before `final`, call `sendMessage()` and complete the same bubble without showing a separate error banner just for streaming failure (D-03). Add restore-toast state in `ChatScreen.tsx` so reopening the latest session shows one subtle auto-dismissing toast after full history loads (D-14). Keep full-history restore behavior (D-11/D-12), keep the existing history modal entry point (D-13), and update scroll handling so active streams auto-scroll only when the user is already near the bottom (~120px threshold). In `useVoiceMode.ts`, ensure TTS starts only after the `final` message text is available; do not auto-play partial streamed text (D-04).
  </action>
  <acceptance_criteria>
    - `senso/src/features/coaching/ChatScreen.tsx` contains stream-handling state for progressive assistant text updates.
    - `senso/src/features/coaching/ChatScreen.tsx` no longer relies solely on the skeleton bubble while a streamed reply is active.
    - `senso/src/features/coaching/ChatScreen.tsx` includes restore-toast state and auto-dismiss timing.
    - `senso/src/features/coaching/useVoiceMode.ts` still defers audio playback until full reply text exists.
  </acceptance_criteria>
  <verify>
    <automated>docker compose run --rm frontend pnpm build</automated>
  </verify>
  <done>The chat UI streams the main text inside a real assistant bubble, falls back silently to the old path when needed, preserves full-history restore, and keeps voice playback final-only.</done>
</task>

</tasks>

<verification>
Run:
- `docker compose run --rm frontend pnpm build`
</verification>

<success_criteria>
- The assistant message can appear progressively in the main bubble while structured extras wait for the final payload.
- Returning to the chat restores the latest full conversation and shows a subtle continuity cue.
</success_criteria>

<output>
After completion, create `.planning/phases/07-streaming-nice-to-have-polish/07-03-SUMMARY.md`
</output>
