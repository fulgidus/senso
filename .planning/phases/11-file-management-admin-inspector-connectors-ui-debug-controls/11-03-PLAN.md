---
phase: 11-file-management-admin-inspector-connectors-ui-debug-controls
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - senso/src/features/profile/ProfileScreen.tsx
  - senso/src/features/profile/FilesTab.tsx
  - senso/src/features/profile/AdminInspectorDrawer.tsx
  - senso/src/api/ingestionFilesApi.ts
  - senso/src/i18n/locales/it.json
  - senso/src/i18n/locales/en.json
autonomous: true
requirements: [FILE-01, FILE-02]

must_haves:
  truths:
    - "ProfileScreen has a 4th tab 'files' alongside summary/charts/timeline"
    - "Files tab lists uploaded files with filename, status badge, date, size, document_type"
    - "Each file row has retry and delete action buttons"
    - "Empty state shown when no files"
    - "Admin users see an Inspect button on each file row"
    - "Inspect button opens a full-screen drawer with upload metadata, ExtractedDocument, payload_json tree, transactions list, and pipeline trace steps"
    - "Any node in the inspector tree has a copy-to-clipboard button"
    - "All new strings use i18n keys from it.json / en.json"
  artifacts:
    - path: "senso/src/features/profile/FilesTab.tsx"
      provides: "Files tab component with file list and actions"
      min_lines: 80
    - path: "senso/src/features/profile/AdminInspectorDrawer.tsx"
      provides: "Full-screen admin inspector drawer"
      min_lines: 120
    - path: "senso/src/api/ingestionFilesApi.ts"
      provides: "API calls for files tab: listUploads, deleteUpload, retryUpload, getExtracted, getTrace"
      exports: ["listUploads", "deleteUpload", "retryUpload", "getExtracted", "getTrace"]
    - path: "senso/src/i18n/locales/it.json"
      provides: "i18n strings for files tab and inspector"
      contains: "files"
  key_links:
    - from: "senso/src/features/profile/ProfileScreen.tsx"
      to: "senso/src/features/profile/FilesTab.tsx"
      via: "renders FilesTab when activeTab === 'files'"
      pattern: "FilesTab"
    - from: "senso/src/features/profile/FilesTab.tsx"
      to: "senso/src/api/ingestionFilesApi.ts"
      via: "calls listUploads, deleteUpload, retryUpload on load/action"
      pattern: "listUploads"
    - from: "senso/src/features/profile/AdminInspectorDrawer.tsx"
      to: "senso/src/api/ingestionFilesApi.ts"
      via: "calls getExtracted and getTrace on open"
      pattern: "getExtracted|getTrace"
---

<objective>
Add "Your Files" tab to ProfileScreen and the admin pipeline inspector drawer.

Purpose: Let users manage their uploaded documents (retry, delete) and let admins inspect the full extraction pipeline state for any upload - critical for debugging during demo.
Output: FilesTab component, AdminInspectorDrawer, ingestionFilesApi, wired into ProfileScreen. All strings i18n-safe.
</objective>

<execution_context>
@/home/fulgidus/Documents/senso/.opencode/get-shit-done/workflows/execute-plan.md
@/home/fulgidus/Documents/senso/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@senso/src/features/profile/ProfileScreen.tsx
@senso/src/lib/api-client.ts
@senso/src/lib/profile-api.ts
@senso/src/features/auth/types.ts
@senso/src/i18n/locales/it.json

<interfaces>
<!-- Key existing contracts the executor needs -->

From senso/src/features/profile/ProfileScreen.tsx:
```typescript
// Current tab state:
const [activeTab, setActiveTab] = useState<"summary" | "charts" | "timeline">("summary")

// Tab buttons rendered at line ~265:
// onClick={() => setActiveTab("summary")} / "charts" / "timeline"

// Component receives:
type Props = {
  user: User   // has user.isAdmin?: boolean, user.role?: string
  token: string
  onAddDocuments: () => void
  onNavigateToChat: () => void
  onSignOut?: () => Promise<void>
  onNoProfile?: () => void
  onRetrigger?: () => void
}
```

From senso/src/lib/api-client.ts:
```typescript
// Pattern used throughout the codebase:
export async function apiRequest<T>(
  base: string,
  path: string,
  options?: RequestInit & { token?: string }
): Promise<T>
```

From senso/src/features/auth/types.ts:
```typescript
export type User = {
    id: string
    email: string
    firstName: string | null
    isAdmin?: boolean
    role?: string  // added in Plan 01
    // ...
}
```

Existing ingestion endpoints (already implemented in api):
- GET /ingestion/uploads → list[UploadStatusDTO]
- DELETE /ingestion/uploads/{id} → 204
- POST /ingestion/uploads/{id}/retry → {upload_id, status}
- GET /ingestion/uploads/{id}/extracted → ExtractedDocument dict
- GET /admin/ingestion/uploads/{id}/trace → list[IngestionTraceDTO]  (admin only, from Plan 02)

UploadStatusDTO shape (from api/app/schemas/ingestion.py):
```typescript
// Maps to these frontend fields (snake_case from API):
type UploadFile = {
  id: string
  original_filename: string  // note: API returns snake_case per existing convention
  content_type: string
  size_bytes: number
  uploaded_at: string
  extraction_status: string  // "pending"|"queued"|"processing"|"done"|"failed"
  extraction_method: string | null
  module_source: string | null
  confirmed: boolean
  document_type?: string  // from ExtractedDocument if available
}
```

Backend returns snake_case (per established D-14 convention). Use snake_case in TypeScript interfaces.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ingestionFilesApi.ts and FilesTab.tsx component</name>
  <files>senso/src/api/ingestionFilesApi.ts, senso/src/features/profile/FilesTab.tsx, senso/src/i18n/locales/it.json, senso/src/i18n/locales/en.json</files>
  <action>
    Create senso/src/api/ingestionFilesApi.ts:
    - Use `apiRequest` from `@/lib/api-client` and `getBackendBaseUrl` from `@/lib/config`
    - Export types: `UploadFile` (id, original_filename, content_type, size_bytes, uploaded_at, extraction_status, extraction_method, module_source, confirmed), `ExtractedDocumentDetail` (document_type, module_name, confidence, raw_text, payload_json, extracted_at), `TraceStep` (id, step_name, step_order, input_summary, output_summary, raw_input, raw_output, duration_ms, status, created_at)
    - Export functions:
      - `listUploads(token: string): Promise<UploadFile[]>` → GET /ingestion/uploads
      - `deleteUpload(token: string, uploadId: string): Promise<void>` → DELETE /ingestion/uploads/{id}
      - `retryUpload(token: string, uploadId: string): Promise<{upload_id: string; status: string}>` → POST /ingestion/uploads/{id}/retry with body `{hint: null}`
      - `getExtracted(token: string, uploadId: string): Promise<ExtractedDocumentDetail | null>` → GET /ingestion/uploads/{id}/extracted (return null on 404)
      - `getTrace(token: string, uploadId: string): Promise<TraceStep[]>` → GET /admin/ingestion/uploads/{id}/trace (admin only; return [] on 403/404)

    Create senso/src/features/profile/FilesTab.tsx:
    - Props: `{ token: string; isAdmin: boolean }`
    - On mount: call listUploads(token) and setFiles(result)
    - Renders: loading skeleton while fetching, empty state with i18n key `files.empty` when no files
    - File list (ul): each file row shows:
      - filename (original_filename), truncated to 40 chars with title for full name
      - Status badge: colored pill using extraction_status - pending/queued=yellow, processing=blue, done=green, failed=red
      - Upload date formatted as it-IT locale date string
      - File size in KB or MB (format with 1 decimal)
      - document_type if present (from the file's payload or from a loaded ExtractedDocument - for simplicity, fetch this only in the admin drawer; show "-" here)
    - Per-file action buttons:
      - Retry (only when status is "failed" or "pending"): calls retryUpload, then refreshes list. Icon: RefreshCw from lucide-react
      - Delete: calls deleteUpload with window.confirm (i18n key `files.confirmDelete`), then removes from list. Icon: Trash2 from lucide-react
      - Inspect (only when isAdmin): opens AdminInspectorDrawer for that upload. Icon: Search from lucide-react
    - All button labels and aria labels from i18n keys. No hardcoded Italian/English strings.
    - Error state: show i18n `files.loadError` with retry button

    Add to senso/src/i18n/locales/it.json under a new "files" key:
    ```json
    "files": {
      "tabLabel": "I tuoi file",
      "empty": "Nessun file caricato.",
      "confirmDelete": "Eliminare questo file? L'azione è irreversibile.",
      "loadError": "Impossibile caricare i file. Riprova.",
      "retry": "Riprova",
      "delete": "Elimina",
      "inspect": "Ispeziona",
      "statusPending": "In attesa",
      "statusQueued": "In coda",
      "statusProcessing": "In elaborazione",
      "statusDone": "Completato",
      "statusFailed": "Errore",
      "sizeKb": "{{size}} KB",
      "sizeMb": "{{size}} MB"
    }
    ```

    Add equivalent keys to senso/src/i18n/locales/en.json:
    ```json
    "files": {
      "tabLabel": "Your files",
      "empty": "No files uploaded.",
      "confirmDelete": "Delete this file? This action cannot be undone.",
      "loadError": "Could not load files. Please try again.",
      "retry": "Retry",
      "delete": "Delete",
      "inspect": "Inspect",
      "statusPending": "Pending",
      "statusQueued": "Queued",
      "statusProcessing": "Processing",
      "statusDone": "Done",
      "statusFailed": "Failed",
      "sizeKb": "{{size}} KB",
      "sizeMb": "{{size}} MB"
    }
    ```
  </action>
  <verify>
    <automated>docker compose run --rm frontend pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>ingestionFilesApi.ts exports all 5 functions, FilesTab.tsx renders file list with status badge, retry/delete/inspect buttons, i18n keys added to both locales</done>
</task>

<task type="auto">
  <name>Task 2: Create AdminInspectorDrawer and wire files tab into ProfileScreen</name>
  <files>senso/src/features/profile/AdminInspectorDrawer.tsx, senso/src/features/profile/ProfileScreen.tsx, senso/src/i18n/locales/it.json, senso/src/i18n/locales/en.json</files>
  <action>
    Create senso/src/features/profile/AdminInspectorDrawer.tsx:
    - Props: `{ uploadId: string; token: string; onClose: () => void }`
    - On mount: call getExtracted(token, uploadId) and getTrace(token, uploadId) in parallel
    - Renders as a fixed full-screen overlay (z-50, bg-background/95 backdrop, overflow-y-auto) with a close button (X icon, top-right)
    - Sections (each collapsible with a toggle chevron):
      1. **Upload metadata**: shows upload_id, extraction_status, extraction_method, module_source, uploaded_at, size_bytes
      2. **Extracted document**: shows document_type, module_name, confidence (as %), raw_text (pre-wrapped in a scrollable box), extracted_at
      3. **Payload JSON**: renders payload_json as a collapsible nested JSON tree. Use a simple recursive `JsonTree` sub-component that renders each key-value pair. Arrays and objects get a toggle button (chevron) to expand/collapse. Leaf values shown as colored text (strings=green, numbers=blue, booleans=orange, null=gray).
      4. **Transactions** (only when document_type includes "bank_statement"): list each transaction (date, description, amount, currency, category). Each row expandable to show full JSON.
      5. **Pipeline trace**: ordered list of trace steps. Each step shows: step_order, step_name, status badge (success=green/error=red/skipped=gray), duration_ms, input_summary, output_summary. Expandable to show raw_input and raw_output.
    - **Copy button on every node**: each section header has a "copy JSON" button (Clipboard icon from lucide-react). Clicking copies that section serialized as JSON to clipboard via `navigator.clipboard.writeText()`. A "whole tree" copy button at the top copies all sections combined.
    - Loading skeleton shown while fetching.
    - Error state: if getExtracted returns null, show i18n `inspector.noExtracted`.
    - All labels from i18n keys under "inspector" namespace.

    Add to senso/src/i18n/locales/it.json under "inspector" key:
    ```json
    "inspector": {
      "title": "Ispezione pipeline",
      "close": "Chiudi",
      "copySection": "Copia JSON",
      "copyAll": "Copia tutto",
      "copied": "Copiato!",
      "sectionUpload": "Metadati upload",
      "sectionExtracted": "Documento estratto",
      "sectionPayload": "Payload JSON",
      "sectionTransactions": "Transazioni",
      "sectionTrace": "Trace pipeline",
      "noExtracted": "Nessun documento estratto disponibile.",
      "confidence": "Confidenza",
      "durationMs": "{{ms}} ms"
    }
    ```
    Add English equivalents to en.json.

    Surgical edit to senso/src/features/profile/ProfileScreen.tsx:
    1. Update tab type: `useState<"summary" | "charts" | "timeline" | "files">("summary")`
    2. Add "files" tab button in the tab bar (after timeline, before the closing div). Use i18n key `files.tabLabel`. Match existing tab button pattern exactly.
    3. Import FilesTab from `./FilesTab` and AdminInspectorDrawer from `./AdminInspectorDrawer`
    4. Add `const [inspectUploadId, setInspectUploadId] = useState<string | null>(null)` state
    5. In the tab content area, add: `{activeTab === "files" && <FilesTab token={token} isAdmin={!!_user.isAdmin || _user.role === "admin"} />}`
    6. Render `{inspectUploadId && <AdminInspectorDrawer uploadId={inspectUploadId} token={token} onClose={() => setInspectUploadId(null)} />}` at the bottom of the component (before closing `</main>`)
    7. Pass `onInspect={setInspectUploadId}` prop to FilesTab: update FilesTab Props to include `onInspect?: (uploadId: string) => void` and call it when Inspect is clicked

    IMPORTANT: surgical edits only - do NOT rewrite ProfileScreen. Add the minimum lines needed.
  </action>
  <verify>
    <automated>docker compose run --rm frontend pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>AdminInspectorDrawer renders JSON tree with copy buttons, ProfileScreen has "files" 4th tab, FilesTab is wired to AdminInspectorDrawer via onInspect, build passes with no TS errors</done>
</task>

</tasks>

<verification>
- docker compose run --rm frontend pnpm build - no TypeScript errors
- ProfileScreen renders "I tuoi file" tab button
- FilesTab shows empty state or file list
- Admin Inspect button visible only for isAdmin users
- Inspector drawer renders with sections and copy buttons
</verification>

<success_criteria>
- 4 tabs in ProfileScreen: summary, charts, timeline, files
- Files tab: file list with status badge, retry, delete, inspect (admin only) per row
- Empty state when no uploads
- AdminInspectorDrawer: 5 sections, recursive JSON tree, copy-to-clipboard buttons
- All user-facing strings in it.json + en.json
- Frontend builds with no TS errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-file-management-admin-inspector-connectors-ui-debug-controls/11-03-SUMMARY.md`
</output>
