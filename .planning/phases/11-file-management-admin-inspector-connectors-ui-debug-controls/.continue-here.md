---
phase: 11-file-management-admin-inspector-connectors-ui-debug-controls
task: 3
total_tasks: 4
status: in_progress
last_updated: 2026-03-31T16:43:39.437Z
---

<current_state>
Phase 11 is mid-execution. Wave 1 (plans 11-01 and 11-02) completed successfully. Wave 2 was being executed in parallel when the session was interrupted. Plan 11-03 is partially done - Task 1 committed, Task 2 not started. Plan 11-04 is partially done - backend debug.py created and main.py patched (uncommitted), frontend work not started.
</current_state>

<completed_work>

- Plan 11-01: RBAC role column - DONE (commits e43a3a5, f1f4bb8, e3a7e08; SUMMARY exists)
- Plan 11-02: Ingestion pipeline tracing - DONE (commits a870328, 391ddcc; SUMMARY exists)
- Plan 11-03, Task 1: ingestionFilesApi.ts + FilesTab.tsx + i18n keys - DONE (commit 9bf9d35)
  - senso/src/api/ingestionFilesApi.ts created (listUploads, deleteUpload, retryUpload, getExtracted, getTrace)
  - senso/src/features/profile/FilesTab.tsx created with status badges, retry/delete/inspect actions
  - i18n keys added to it.json and en.json for files and inspector
- Plan 11-04, partial: api/app/api/debug.py created (untracked), api/app/main.py patched with debug_router import and include_router (unstaged)
</completed_work>

<remaining_work>

- Plan 11-03, Task 2: Wire FilesTab into ProfileScreen (add "files" tab), build AdminInspectorDrawer.tsx, create SUMMARY.md, update STATE.md + ROADMAP.md
  - ProfileScreen.tsx needs a 4th tab "files" rendering FilesTab when activeTab === 'files'
  - AdminInspectorDrawer.tsx needs to be created (full-screen drawer, min 120 lines) with upload metadata, ExtractedDocument, payload_json tree, transactions list, pipeline trace steps, copy-to-clipboard on each node
- Plan 11-04, Task 1 (partial): api/app/api/debug.py exists but must be committed - verify it has POST /debug/restart-ingestion, POST /debug/purge-coaching, DELETE /debug/nuke (all require_tester). api/app/main.py patch is unstaged.
- Plan 11-04, Task 2: Frontend for 11-04
  - ConnectorsTab.tsx - static grid of bank-connection cards with "Prossimamente" badges (min 40 lines)
  - ProfileScreen.tsx - add 5th "connectors" tab rendering ConnectorsTab
  - DebugScreen.tsx - 3 sections: ingestion restart, purge controls, nuke (min 80 lines)
  - SettingsScreen.tsx - "Strumenti sviluppatore" link only for role tester|admin
  - App.tsx - Route /debug → DebugScreen, guarded by role tester|admin, redirect to / if unauthorized
  - i18n keys in it.json / en.json for connectors and debug namespaces
  - SUMMARY.md for 11-04, STATE.md + ROADMAP.md update
</remaining_work>

<decisions_made>

- Wave 1 ran fully parallel (no conflicts - 11-01 and 11-02 touched overlapping files: models.py, session.py - 11-02 agent detected 11-01 had already created IngestionTrace model and skipped that task)
- Wave 2 was spawned parallel but the session was aborted after 11-03 Task 1 committed and 11-04 partial backend work was done (debug.py created, main.py patched but not committed)
- RBAC: role column uses enum string (user/tester/moderator/admin), not boolean is_admin
- Tracing: _record_trace() never raises - purely additive, fire-and-forget
- Debug endpoints: gated by require_tester (tester or admin role)
</decisions_made>

<blockers>

- No hard blockers. Session was interrupted (tool execution aborted) mid-Wave 2.
- api/app/main.py has unstaged changes (debug_router import + include_router) that need committing as part of 11-04 Task 1
- api/app/api/debug.py is untracked - needs to be committed
</blockers>

<context>
The execute-phase orchestrator spawned two Wave 2 agents in parallel for 11-03 and 11-04. Both returned "Tool execution aborted" before completing. However partial work was committed/created:
- 11-03: Task 1 committed (9bf9d35). Task 2 (AdminInspectorDrawer + wire ProfileScreen) not done, no SUMMARY.
- 11-04: debug.py created untracked, main.py patched unstaged. No frontend work done, no SUMMARY.
The gsd-executor agents were mid-flight when aborted. Resume should complete each plan from where it left off.
</context>

<next_action>
Resume Wave 2 execution. Re-run `/gsd-execute-phase 11` - the orchestrator will detect 11-01 and 11-02 have SUMMARYs and skip them, then spawn executors for 11-03 and 11-04. The executors should detect partial work (committed files for 11-03 Task 1; uncommitted files for 11-04) and continue from the right point. Alternatively run interactively: complete 11-03 Task 2 (AdminInspectorDrawer + ProfileScreen wiring + SUMMARY), then complete 11-04 (commit debug.py + main.py, build all frontend components + SUMMARY).
</next_action>
